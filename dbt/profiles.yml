toronto_open_data:
  target: dev
  outputs:
    dev:
      type: postgres
      host: "{{ env_var('POSTGRES_HOST', 'localhost') }}"
      port: "{{ env_var('POSTGRES_PORT', '5433') | int }}"
      user: "{{ env_var('POSTGRES_USER', 'toronto') }}"
      password: "{{ env_var('POSTGRES_PASSWORD', 'toronto') }}"
      dbname: "{{ env_var('POSTGRES_DB', 'toronto') }}"
      schema: public
      threads: 4
