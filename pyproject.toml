[tool.ruff]
line-length = 120
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "I", "B", "UP"]
# B008: FastAPI's Query(...) in defaults is the framework's idiom
extend-ignore = ["B008"]

[tool.pytest.ini_options]
testpaths = ["tests"]
